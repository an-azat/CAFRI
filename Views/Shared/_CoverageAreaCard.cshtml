@model CAFRI.ViewModels.About.AboutSectionCardViewModel

<article class="coverage-card">
    <div class="coverage-card__icon">@Model.Title[..1]</div>
    <h3>@Model.Title</h3>
    <p>@Model.Description</p>
</article>
