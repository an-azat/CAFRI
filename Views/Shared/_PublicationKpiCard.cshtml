@model CAFRI.ViewModels.Publications.PublicationKpiCardViewModel

<article class="kpi-card kpi-card--@Model.Tone">
    <div class="kpi-card__label">@Model.Label</div>
    <div class="kpi-card__value">@Model.Value</div>
    <div class="kpi-card__detail">@Model.Detail</div>
</article>
