@model CAFRI.ViewModels.Countries.CountrySnapshotCardViewModel

<article class="country-summary-card">
    <div class="country-summary-card__label">@Model.Label</div>
    <div class="country-summary-card__value">@Model.Value</div>
    <div class="country-summary-card__description">@Model.Description</div>
</article>
