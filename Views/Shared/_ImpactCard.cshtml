@model CAFRI.ViewModels.Intelligence.IntelligenceImpactCardViewModel

<article class="impact-card">
    <h3>@Model.Title</h3>
    <p>@Model.Description</p>
    <div class="impact-card__label @Model.ImpactTone">@Model.ImpactLabel</div>
</article>
