@model CAFRI.ViewModels.About.AboutSectionCardViewModel

<article class="source-item">
    <div class="source-item__icon">@Model.Title[..1]</div>
    <h3>@Model.Title</h3>
    <p>@Model.Description</p>
</article>
